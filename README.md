# APD — Agência de Presença Digital

Portfólio institucional da APD.

## Stack
React + TypeScript + Vite + CSS + Lucide React

## Rodar localmente
npm install
npm run dev

## Build
npm run build

Antes de publicar, ajuste os links de Instagram, WhatsApp e e-mail no objeto `LINKS` em `src/App.tsx`.
